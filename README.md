To run vagrant-jenkins, 

    git clone http://github.com/cwalker67/vagrant-jenkins.git
    cd vagrant-jenkins

Get needed submodules

    git submodule init
    git submodule update

Fire up vagrant VM

    vagrant up
